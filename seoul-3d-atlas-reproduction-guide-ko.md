# Seoul 3D Atlas 재현 가이드

댓글에서 많은 분이 어떤 프롬프트를 사용했는지, 프로젝트에 토큰이 얼마나 소모됐는지 물어보았다. 그래서 다른 사람도 비슷한 애플리케이션을 재현할 수 있도록 백서 형식으로 제작 과정을 공유한다.

## 1. 처음 사용하는 사용자는 프롬프트를 어떻게 작성해야 하는가?

처음 실험했을 때는 단 한 문장만으로 전체 프로젝트를 시작할 수 있었다. 그러나 다른 사람이 비슷한 결과를 안정적으로 재현하려면 프롬프트에 제품 목표, 데이터 출처, 기능, 성능 요구사항, 검증 기준, 배포 조건을 구체적으로 명시하는 것이 좋다.

### 첫 번째 방법: 한 문장으로 시작하기

기존 Codex 아키텍처와 GPT-6 Astra 기반 Codex는 차이가 있다. 따라서 먼저 다음 지시문으로 시작하는 것을 권장한다.

> 기존 Codex의 `AGENTS.md` 파일과 Skills를 검토하고 업그레이드한 뒤, GPT-6 Astra 기반 Codex에 맞게 초기화하라.

그다음 사용할 수 있는 가장 짧은 시작 프롬프트는 다음과 같다.

> 서울 전체의 3D 지도를 만들고 OpenAI Sites에 배포하라.

이 방식은 Codex에 높은 수준의 자율성을 부여한다. Codex가 애플리케이션의 많은 부분을 스스로 계획하고 구축할 수 있다는 사실은 실제로 매우 놀라웠다.

다만 이 방법은 빠르게 결과를 만들 수 있는 대신 데이터 출처, 정확도, 기능 범위, 완료 기준이 실행할 때마다 달라질 수 있다.

### 두 번째 방법: 초기 버전의 핵심 기능을 구체적으로 정의하기

초기 버전의 핵심 기능을 좀 더 안정적으로 재현하려면 다음 프롬프트를 사용한다.

> 사용자가 웹 브라우저에서 서울 전체를 직접 탐색할 수 있는 인터랙티브 미니어처 3D 지도를 만들고 OpenAI Sites에 배포하라.
>
> **제품 목표**
>
> - 사용자가 서울 전체를 한눈에 보고 지도를 회전·확대·축소·이동할 수 있게 한다.
> - 처음 방문한 사용자도 주요 지역을 쉽게 살펴볼 수 있도록 랜드마크 자동 비행을 제공한다.
> - 데스크톱 조작과 모바일 터치 조작을 모두 지원한다.
>
> **지도 범위와 데이터**
>
> - 서울 25개 자치구와 주변 지형을 포함한다.
> - 건물, 도로, 수계에는 OpenStreetMap 기반 공개 벡터 데이터를 사용한다.
> - 가능하면 OpenFreeMap 벡터 타일을 OpenMapTiles 형식으로 전처리해 사용한다.
> - 지형에는 Mapzen 또는 AWS Terrain Tiles와 같은 공개 표고 데이터를 사용한다.
> - 데이터 스냅샷 날짜, 출처, 라이선스를 웹사이트와 README에 기록한다.
> - 건물 위치, 외곽선, 높이에는 원본 데이터를 사용한다. 높이 정보가 없거나 추정한 경우에는 그 사실을 명확히 공개한다.
>
> **초기 기능**
>
> - 서울 25개 자치구 경계를 켜고 끌 수 있게 한다.
> - 주요 랜드마크 14곳을 선정하고 자동 비행으로 차례대로 보여준다.
> - 낮·노을·야경 모드를 제공한다.
> - 한강, 산, 도로, 건물이 시각적으로 구분되게 한다.
> - 가시성을 높이기 위해 지형이나 건물 높이를 과장했다면 배율과 적용 이유를 공개한다.
>
> **기술과 성능**
>
> - Three.js를 사용한 정적 웹사이트로 구축한다.
> - 수십만 개의 건물을 브라우저에서 처리할 수 있도록 공유 Geometry와 `InstancedMesh`를 사용해 렌더링 비용을 제어한다.
> - 건물 데이터를 압축 이진 형식으로 저장하고 레코드 수와 파일 길이를 모두 검증한다.
> - 픽셀 비율, 그림자 해상도, 표시 건물 수를 줄이는 별도의 모바일 렌더링 예산을 적용한다.
> - 실제 FPS를 측정하지 않았다면 구체적인 성능 수치를 주장하지 않는다. 구현한 최적화만 설명한다.
>
> **검증 및 완료 기준**
>
> - 서울 25개 자치구와 처리된 전체 건물 수를 확인한다.
> - 건물 이진 파일의 레코드 길이가 메타데이터와 일치하는지 확인한다.
> - 회전, 확대·축소, 이동, 랜드마크 자동 비행, 낮·노을·야경 전환, 모바일 입력을 테스트한다.
> - 한강과 주요 지형이 건물이나 지도 바닥 아래에 가려지지 않는지 시각적으로 확인한다.
> - 공개 데이터의 한계, 형상 단순화 방식, 적용 라이선스를 README와 웹사이트 정보 패널에 모두 기록한다.
> - 빌드와 검증을 통과한 뒤 OpenAI Sites에 배포하고 공개 URL을 제공한다.
> - 완료 보고서에는 실제 소요 시간, 확인 가능한 토큰 사용량, 데이터 레코드 수, 테스트 결과, 남아 있는 한계를 포함한다.

이 프롬프트에서 가장 중요한 부분은 기술 이름의 목록이 아니다. 목표로 하는 사용자 경험과 프로젝트 완료 여부를 판단하는 기준을 서로 연결했다는 점이 핵심이다.

필수 프롬프트 구성 요소는 다음과 같다.

- **제품 목표:** 서울 전체 보기와 직접 탐색을 명시해 Codex가 인터페이스와 상호작용을 우선하도록 한다.
- **지리적 범위:** 서울 25개 자치구와 주변 지형으로 범위를 한정해 데이터 수집 범위가 불필요하게 확장되는 것을 막는다.
- **데이터 출처:** OpenStreetMap, OpenFreeMap, Terrain Tiles를 지정해 재현 가능성과 명확한 출처 표기 기준을 제공한다.
- **핵심 기능:** 랜드마크 14곳, 자동 비행, 세 가지 환경 모드, 모바일 지원을 초기 완료 기준으로 정의한다.
- **성능 요구사항:** 인스턴싱, 이진 데이터, 별도의 모바일 렌더링 예산을 명시해 브라우저에서 수십만 개 객체를 처리할 방향을 제시한다.
- **검증 기준:** 레코드 수, 파일 길이, UI 동작, 시각 검증을 포함해 단순히 페이지가 열린다는 사실을 넘어 측정 가능한 품질 기준을 만든다.
- **한계 공개:** 추정 높이, 단순화한 형상, 측정하지 않은 FPS를 명시해 사용자가 프로젝트의 정확도나 성능을 오해하지 않도록 한다.
- **배포 결과:** 검증, 배포, 공개 URL, 완료 보고서를 요구해 작업의 최종 상태를 분명하게 만든다.

### 세 번째 방법: 최신 프로젝트를 참고한 퓨샷 프롬프팅

가장 권장하는 방법은 최신 버전의 프로젝트를 참고 사례로 제공하고 퓨샷 프롬프팅을 적용하는 것이다.

GPT-6 Astra 기반 Codex에 다음 프롬프트를 입력한다.

> 다음 사이트의 모든 UI 요소와 기능을 참고하라.
>
> <https://seoul-3d-atlas.synabreu.chatgpt.site/>
>
> 이를 바탕으로 **[도시 이름]**의 인터랙티브 3D 지도를 만들고 OpenAI Sites에 배포한 뒤 버전을 `v1.0`으로 표시하라.

토큰이나 컴퓨팅 용량을 모두 사용했다는 오류가 나타나면 추론 수준을 한 단계 낮춘다. 예를 들어 `Max` 또는 `xhigh`로 설정한 뒤 작업을 다시 실행한다.

배포가 끝나면 애플리케이션을 직접 확인하고 문제를 찾아 후속 지시를 내린다. 그런 다음 다시 배포하고 결과가 기대 수준에 도달할 때까지 이 과정을 반복한다.

Codex가 기억하거나 미리보기로 보여주는 버전과 OpenAI Sites에 실제 배포된 버전이 다르다면 캐시 불일치일 수 있다. Sites에 배포된 버전을 최종 버전으로 간주하고 페이지를 새로 고친 뒤 다시 확인한다.

직접 시도해보길 바란다. 감사합니다.
