| 설정      | 최초 권장값                   | 설명                                                           |
| ------- | ------------------------ | ------------------------------------------------------------ |
| 모델      | `gpt-6-astra`            | GPT-6 Astra의 정확한 모델 식별자다.                                    |
| API     | Responses API            | 도구 호출, 스트리밍, 에이전트 작업에 적합하다. 도구 호출은 Responses API를 사용해야 한다.   |
| 추론 수준   | `low`                    | 빠른 응답과 비용을 확인하기 좋은 시작점이다. `none`은 지원되지 않는다.                  |
| 지시문     | 명확한 `instructions`       | 역할, 작업 범위, 완료 조건, 출력 형식을 구체적으로 지정한다.                         |
| 도구      | 필요한 도구만 등록               | 함수 호출 스키마를 `tools` 필드에 전달한다.                                 |
| 비동기 도구  | 필요할 때만 `async: true`     | 오래 걸리는 도구를 실행하는 동안 다른 작업을 계속할 때 사용한다.                        |
| 스트리밍    | 사용자 화면에 실시간 출력을 보여줄 때 사용 | 일반적인 API 응답만 필요하면 처음에는 생략할 수 있다.                             |
| 구조화된 출력 | JSON 결과가 필요할 때 사용        | 다른 프로그램이 결과를 자동 처리할 때 적합하다.                                  |
| 프롬프트 캐시 | 긴 공통 지시문을 반복할 때 사용       | GPT-5.5 이하에서 이전한다면 `prompt_cache_options.ttl: "30m"`으로 변경한다. |
